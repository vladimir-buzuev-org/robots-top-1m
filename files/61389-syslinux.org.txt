# Explicitly disallow pipermail (which is permitted for some hosts in the
# global configuration) so that the http://syslinux.zytor.com/archives/
# path gets archived.

User-Agent: *
Disallow: /pipermail/
