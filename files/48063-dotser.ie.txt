User-agent: *

# This technically isn't valid, since for some bizarre reason sitemap paths must
# be *absolute*, not relative, but Stack Exchange does this on their sites. If they
# can get away with it, I can.

Sitemap: /sitemap.xml

